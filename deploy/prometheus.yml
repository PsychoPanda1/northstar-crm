global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: northstar
    metrics_path: /api/metrics
    bearer_token_file: /run/secrets/northstar_metrics
    static_configs:
      - targets: ['northstar:4173']
