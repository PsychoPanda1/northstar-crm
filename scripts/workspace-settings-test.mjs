import { existsSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { spawn } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const root = fileURLToPath(new URL('../', import.meta.url));
const port = 21000 + Math.floor(Math.random() * 1000);
const dataFile = join(tmpdir(), `northstar-workspace-settings-${process.pid}-${Date.now()}.json`);
const env = { ...process.env, NODE_ENV: 'development', NORTHSTAR_ALLOW_DEMO_LOGIN: 'true', PORT: String(port), NORTHSTAR_DATA_FILE: dataFile, NORTHSTAR_SESSION_FILE: `${dataFile}.sessions` };
const base = `http://127.0.0.1:${port}`;
let child;
const request = async (path, options) => { const response = await fetch(`${base}${path}`, options); return { response, body: await response.json().catch(() => ({})) }; };
const postJson = (path, body) => request(path, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(body) });
const waitForServer = async () => { for (let attempt = 0; attempt < 400; attempt += 1) { try { if ((await fetch(`${base}/api/health`)).ok) return; } catch {} await new Promise((resolve) => setTimeout(resolve, 50)); } throw new Error('workspace settings test server did not start'); };
const assert = (condition, message) => { if (!condition) throw new Error(message); };

try {
  child = spawn(process.execPath, ['server.mjs'], { cwd: root, env, stdio: 'ignore' });
  await waitForServer();
  const login = await postJson('/api/auth/demo-login?service=plumbing', { service: 'plumbing', role: 'owner' });
  assert(login.response.ok, 'workspace settings owner login failed');
  const headers = { authorization: `Bearer ${login.body.token}` };
  const initial = await request('/api/settings/workspace', { headers });
  const landingPages = await request('/api/settings/landing-pages', { headers });
  const claimed = await request('/api/settings/landing-pages', { method: 'POST', headers: { ...headers, 'content-type': 'application/json', 'idempotency-key': 'landing-page-key-1' }, body: JSON.stringify({ service: 'commercial-plumbing' }) });
  const claimedDuplicate = await request('/api/settings/landing-pages', { method: 'POST', headers: { ...headers, 'content-type': 'application/json', 'idempotency-key': 'landing-page-key-1' }, body: JSON.stringify({ service: 'commercial-plumbing' }) });
  const handoff = await request('/api/settings/landing-pages/handoff?service=commercial-plumbing', { headers });
  const otherTenantHandoff = await request('/api/settings/landing-pages/handoff?service=electrical', { headers });
  const update = { businessName: 'Clearwater Plumbing Group', serviceLabel: 'Emergency Plumbing', timeZone: 'America/New_York', appointmentMinutes: 75, contactPhone: '(843) 555-0199', contactEmail: 'hello@clearwater.example', serviceArea: 'Charleston and Mount Pleasant', serviceAreaRules: { mode: 'allowlist', enforce: true, cities: ['Charleston', 'Mount Pleasant'], postalCodes: ['29401', '29464'] }, intakeFields: [{ id: 'issue_type', type: 'select', label: 'Issue type', required: true, options: ['Leak', 'Clog', 'Other'] }], messageTemplates: { confirmation: 'You are booked for {service} at {time}.', completed: 'Thanks for trusting {service}. Your visit is complete.' } };
  const updated = await request('/api/settings/workspace', { method: 'PATCH', headers: { ...headers, 'content-type': 'application/json', 'idempotency-key': 'workspace-settings-1' }, body: JSON.stringify(update) });
  const duplicate = await request('/api/settings/workspace', { method: 'PATCH', headers: { ...headers, 'content-type': 'application/json', 'idempotency-key': 'workspace-settings-1' }, body: JSON.stringify(update) });
  const manifest = await request('/api/public/tenant?service=plumbing');
  const claimedManifest = await request('/api/public/tenant?service=commercial-plumbing');
  assert(initial.response.status === 200 && initial.body.workspace?.businessName === 'Clearwater Plumbing', 'workspace settings were not readable');
  assert(landingPages.response.status === 200 && landingPages.body.serviceKeys?.includes('plumbing'), 'landing page keys were not readable');
  assert(claimed.response.status === 201 && claimed.body.service === 'commercial-plumbing' && claimedDuplicate.response.status === 200 && claimedDuplicate.body.duplicate === true && claimedManifest.response.status === 200 && claimedManifest.body.tenant?.slug === 'clearwater-plumbing', 'landing page service key claim was not safe or routable');
  assert(handoff.response.status === 200 && handoff.body.handoff?.version === 2 && handoff.body.handoff?.service === 'commercial-plumbing' && handoff.body.handoff?.clientConfig?.service === 'commercial-plumbing' && handoff.body.handoff?.clientConfig?.manifestPath?.includes('commercial-plumbing') && !JSON.stringify(handoff.body).toLowerCase().includes('password'), 'landing page handoff package was not tenant-safe or copyable');
  assert(otherTenantHandoff.response.status === 404 && otherTenantHandoff.body.error === 'service_key_not_found', 'landing page handoff leaked another tenant service key');
  assert(updated.response.status === 200 && updated.body.workspace?.businessName === update.businessName && updated.body.workspace?.appointmentMinutes === 75 && updated.body.workspace?.contactEmail === update.contactEmail && updated.body.workspace?.serviceArea === update.serviceArea && updated.body.workspace?.intakeFields?.[0]?.id === 'issue_type' && updated.body.workspace?.messageTemplates?.confirmation === update.messageTemplates.confirmation && updated.body.workspace?.messageTemplates?.completed === update.messageTemplates.completed, 'workspace settings update was not applied');
  assert(duplicate.response.status === 200 && duplicate.body.duplicate === true, 'workspace settings update was not idempotent');
  assert(manifest.response.status === 200 && manifest.body.tenant?.businessName === update.businessName && manifest.body.tenant?.serviceLabel === update.serviceLabel && manifest.body.tenant?.appointmentMinutes === 75 && manifest.body.tenant?.contactPhone === update.contactPhone && manifest.body.tenant?.contactEmail === update.contactEmail && manifest.body.tenant?.serviceArea === update.serviceArea, 'public landing manifest did not reflect workspace settings');
  assert(updated.body.workspace?.serviceAreaRules?.enforce === true && updated.body.workspace?.serviceAreaRules?.cities?.includes('charleston') && manifest.body.tenant?.serviceAreaRules?.postalCodes?.includes('29401'), 'service area rules were not normalized or exposed safely');
  const allowedLead = await postJson('/api/public/leads?service=plumbing', { name: 'In-area customer', phone: '843-555-0111', city: 'charleston', location: '1 King Street, Charleston, SC 29401' });
  const blockedLead = await postJson('/api/public/leads?service=plumbing', { name: 'Out-of-area customer', phone: '843-555-0112', city: 'Savannah', location: '1 River Street, Savannah, GA 31401' });
  const availability = await request('/api/public/availability?service=plumbing&days=7');
  const booking = availability.body.slotOptions?.[0] ? await request('/api/public/bookings?service=plumbing', { method: 'POST', headers: { 'content-type': 'application/json', 'idempotency-key': 'workspace-template-booking-1' }, body: JSON.stringify({ name: 'Template booking customer', phone: '843-555-0113', email: 'template@example.test', location: '2 King Street, Charleston, SC 29401', city: 'Charleston', requestedService: 'Emergency Plumbing', slotId: availability.body.slotOptions[0].id }) }) : { response: { status: 0 }, body: {} };
  const assigned = booking.body.id ? await request(`/api/jobs/${encodeURIComponent(booking.body.id)}/assign`, { method: 'POST', headers: { ...headers, 'content-type': 'application/json', 'idempotency-key': 'workspace-template-assignment-1' }, body: JSON.stringify({ technician: 'Alex Rivera' }) }) : { response: { status: 0 }, body: {} };
  const messages = await request('/api/messages', { headers });
  const coverageAllowed = await request('/api/public/coverage?service=plumbing&city=Charleston');
  const coverageBlocked = await request('/api/public/coverage?service=plumbing&postalCode=31401');
  assert(allowedLead.response.status === 201 && blockedLead.response.status === 422 && blockedLead.body.error === 'outside_service_area' && booking.response.status === 201 && assigned.response.status === 200 && messages.body.items?.some((item) => item.jobId === booking.body.id && item.message === 'You are booked for Emergency Plumbing at ' + booking.body.appointment.label + '.') && coverageAllowed.response.status === 200 && coverageAllowed.body.eligible === true && coverageBlocked.body.eligible === false && coverageBlocked.body.reason === 'outside_service_area', 'service area rules or customer notification templates did not qualify public bookings and leads');
  const invalid = await request('/api/settings/workspace', { method: 'PATCH', headers: { ...headers, 'content-type': 'application/json' }, body: JSON.stringify({ appointmentMinutes: 5 }) });
  assert(invalid.response.status === 422, 'invalid workspace settings were accepted');
  const invalidIntake = await request('/api/settings/workspace', { method: 'PATCH', headers: { ...headers, 'content-type': 'application/json' }, body: JSON.stringify({ intakeFields: [{ id: 'issue_type', type: 'select', label: 'Issue type' }] }) });
  assert(invalidIntake.response.status === 422 && invalidIntake.body.error === 'invalid_intake_fields', 'invalid guided intake fields were accepted');
  const invalidTemplates = await request('/api/settings/workspace', { method: 'PATCH', headers: { ...headers, 'content-type': 'application/json' }, body: JSON.stringify({ messageTemplates: { confirmation: '' } }) });
  assert(invalidTemplates.response.status === 422 && invalidTemplates.body.error === 'invalid_message_templates', 'invalid message templates were accepted');
  console.log('Northstar workspace settings test passed');
} finally {
  if (child && !child.killed) child.kill();
  for (const file of [dataFile, `${dataFile}.sessions`, `${dataFile}.tmp`, `${dataFile}.backup`]) if (existsSync(file)) rmSync(file, { force: true });
}
